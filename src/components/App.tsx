import * as React from "react";
import './App.sass';
import './Meow.sass';

export const App: React.FC = () => {

    return (
        <div>
            <h1>My React App!</h1>
        </div>
    );
}

